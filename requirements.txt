# CPU-only torch. Without this, pip on Linux resolves the default torch wheel
# and pulls ~3GB of nvidia-* CUDA libraries that a CPU host can never execute.
# That bloat is why builds have failed since this file gained its ML
# dependencies (892aaa1, 2026-09-01), leaving production on the Phase 0 mock.
# Declared here rather than only in the Dockerfile so it applies whichever
# builder runs -- Docker, Nixpacks, or a plain `pip install -r`.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1+cpu

pytest>=8.0
pandas>=2.0
qdrant-client>=1.19
sentence-transformers>=6.0
transformers>=4.40
sentencepiece>=0.2
FlagEmbedding>=1.3
google-generativeai>=0.8
groq>=0.11
supabase>=2.7
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
python-dotenv==1.0.1
httpx==0.27.0
