# Project status

**Current phase:** Phase 1 — Retrieval spike

## Live services

| Service | URL | State |
|---|---|---|
| API | https://naari-ai-production.up.railway.app | Live — `/health` 200, `/ask` returns mock in contract shape |
| Web app | — | Not deployed yet |
| Model inference | — | Not deployed yet (planned: Hugging Face Space) |

## Who's on what

| Person | Currently | Blocked on |
|---|---|---|
| Sana | Retrieval core — normaliser, embed, search, schema, FakeRetriever. Needs pushing to the repo | — |
| Sabiha | Danger-sign gate merged. API live on Railway | — |
| Tooba | Web app not started — build against the Railway URL | — |
| Mahnoor | Eval sets in progress; clinical reviewer outreach not started | — |

## Latest eval numbers

None reported. The first gold set was generated by the same method as the query
variants, so it is quarantined until it can be rebuilt from independently
harvested questions. `eval/run_eval.py` reports metrics as unavailable rather
than producing a number.

## Variant review burn-down

150 of ~6,400 generated (smoke test on answer_id 1–30). Review not started.

## Open watch items

- **Railway credit.** Free plan is $1/month of credit, trial is $5 one-time. An
  always-on service will exhaust this. Check the usage page weekly.
- **Railway RAM.** Free plan gives 0.5 GB per service, which will not hold bge-m3
  plus the reranker. Model inference needs to live somewhere else — see
  `docs/adr/0001-stack-decisions.md`.

---
*Update this file when reality changes, not on a schedule.*
